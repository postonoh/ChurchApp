<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:converters="clr-namespace:ChurchApp.Converters;assembly=ChurchApp"
             x:Class="ChurchApp.App">
    <Application.Resources>

        <Color x:Key="AppBackgroundColor">#60229a</Color>

        <Style TargetType="{x:Type ContentPage}"
               ApplyToDerivedTypes="True">
            <Setter Property="BackgroundColor"
                    Value="{StaticResource AppBackgroundColor}" />
        </Style>
        
        <ResourceDictionary>
            <converters:ImageSourceConverter x:Key="ImageSourceConverter"/>
            <converters:NullOrEmptyListToIsVisibleConverter x:Key="NullOrEmptyListToIsVisibleConverter"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>