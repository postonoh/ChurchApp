<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:controls="clr-namespace:ChurchApp.Controls"
             xmlns:converters="clr-namespace:ChurchApp.Converters"
             xmlns:extensions="clr-namespace:ChurchApp.Extensions"
             x:Class="ChurchApp.Views.PostDetailPage" Title="{extensions:Translate PostDetailPageTitle}">
    <ContentPage.Resources>
        <Style TargetType="{x:Type ListView}">
            <Setter Property="BackgroundColor"
                    Value="{StaticResource AppBackgroundColor}" />
        </Style>
        <ResourceDictionary>
            <converters:HtmlWebviewConverter x:Key="HtmlWebviewConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <controls:ExternalWebView Source="{Binding SelectedPost, Converter={StaticResource HtmlWebviewConverter}}"
                                  Margin="10" />
    </ContentPage.Content>
</ContentPage>